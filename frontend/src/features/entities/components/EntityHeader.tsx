import React from 'react';
// Placeholder component; real implementation can wrap existing UI pieces from pages.
export function EntityHeader({ name }: { name: string }) {
  return <h2>{name}</h2>;
}
export default EntityHeader;
