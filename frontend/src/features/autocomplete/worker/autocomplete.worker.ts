// Placeholder; actual logic resides in autocomplete.worker.js (JS). This file exists
// to satisfy proposed structure until we fully migrate worker to TypeScript.
export const placeholder = true;
export default placeholder;
