{
  "name": "scoracle-root",
  "private": true,
  "scripts": {
    "install": "cd astro-frontend && npm install",
    "build": "cd astro-frontend && npm run build",
    "dev": "cd astro-frontend && npm run dev",
    "check": "cd astro-frontend && npm run check",
    "preview": "cd astro-frontend && npm run preview"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
